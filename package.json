{
  "name": "interact-monorepo",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm --filter docs dev",
    "build": "pnpm --filter interact build && pnpm --filter docs build"
  },
  "devDependencies": {
    "@alloc/quick-lru": "^5.2.0",
    "@tailwindcss/postcss": "^4",
    "tailwindcss": "^4"
  }
}
